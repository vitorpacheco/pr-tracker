# Every push to main publishes a "nightly" pre-release, replacing the previous
# one. The nightly tag is moved to the new commit.
name: Nightly

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: nightly
  cancel-in-progress: true

jobs:
  version:
    name: Definir versão
    runs-on: ubuntu-latest
    outputs:
      value: ${{ steps.version.outputs.value }}
    steps:
      - name: Versão
        id: version
        run: echo "value=nightly-$(date -u +%Y%m%d)-${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

  ci:
    needs: version
    uses: ./.github/workflows/ci.yml
    with:
      version: ${{ needs.version.outputs.value }}

  release:
    name: Publicar nightly
    needs: [version, ci]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: Baixar pacotes desktop
        uses: actions/download-artifact@v8
        with:
          pattern: pr-tracker-desktop-*
          path: dist
          merge-multiple: true
      - run: make package VERSION="$VERSION"
        env:
          VERSION: ${{ needs.version.outputs.value }}
      - name: Publicar release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          VERSION: ${{ needs.version.outputs.value }}
        run: |
          {
            echo "Build automático do commit ${GITHUB_SHA::7} na \`main\`:"
            echo
            echo "> $(git log -1 --format=%s)"
            echo
            echo "Versão: \`$VERSION\`. Builds nightly são instáveis; para uso normal prefira uma versão \`v*\`."
          } > notes.md
          gh release delete nightly --cleanup-tag --yes || true
          gh release create nightly \
            dist/*.tar.gz dist/*.zip dist/checksums.txt \
            --target "$GITHUB_SHA" \
            --prerelease --latest=false \
            --title "Nightly ($VERSION)" \
            --notes-file notes.md
